name: ATLAS Korea CI

on:
  push:
    branches:
      - feat/atlas-korea-product-publisher
  pull_request:
    branches:
      - feat/revenue-content-pipeline-v1

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run build
