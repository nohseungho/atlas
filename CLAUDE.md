@AGENTS.md

## 보고 언어 규칙 (고정)

- 모든 진행 상황 보고와 최종 보고는 반드시 한국어로 작성한다.
- 코드·커밋 메시지·파일 경로·명령어는 원문 그대로 두되, 설명과 결과 요약은 한국어로 쓴다.

## 공개 상태 안전 규칙 (2026-09-24 긴급 정리 이후)

- 사용자 명시적 승인 전에는 Naver/Blogger 신규 발행·재공개·공개 글 수정을 하지 않는다.
- 자동 얼굴 점수(ArcFace)만으로 이미지를 승인하지 않는다. 최종 승인은 사용자가 이미지를 보고 한다.
- 새로 생성한 이미지는 검수 대기 폴더(`public/atlas/<channel>/<character>/_review/`)에만 둔다.
- 기존 공개 글의 본문과 URL은 보존한다. 비공개 전환 전에는 `data/atlas/backups/`에 백업한다.

## 이미지 검수 기준 (2026-09-24 갱신)

공통: 얼굴만 따로 붙인 듯한 합성, 기괴한 얼굴, 같은 얼굴 크롭·같은 구도 반복, 카드형·PPT형 이미지는 금지.
자동 얼굴 점수만으로 승인하지 않는다. 모든 이미지는 육안 검수 후 사용자 승인을 받는다.

해외 미지(실사):
- 공통 레퍼런스는 `public/atlas/characters/references/miji/`의 얼굴 크롭 6장(마스터 + 승인된 art_021 세트 5장).
- 얼굴형·눈매·코/입 비율·웃는 인상·전체 인물 느낌은 같은 사람처럼 유지한다.
- 메이크업은 연한 버전과 조금 진한 버전 모두 허용하고, 헤어는 긴 생머리·웨이브·묶음·반묶음·모자 등으로 다양화한다.
- 다른 사람처럼 보이거나 한 컷에 같은 인물이 둘 이상 나오면 탈락.

국내(애니메이션):
- 캐릭터 성별보다 이미지 품질과 장면 적합성이 우선이다(남녀 애니메이션 모두 검수 대상).
  단, 현재 채널 잠금 규칙(AGENTS.md, character-channel-policy.js: 국내는 수호)은 그대로이므로,
  수호가 아닌 캐릭터를 실제 발행하려면 사용자가 채널 잠금 변경을 먼저 승인해야 한다.
- 생활 장면형으로 만들고, 글 내용과 맞는 행동·상황이 보여야 한다.
- 제품 글은 실사 제품 사진 + 애니메이션 장면 혼합 가능.
