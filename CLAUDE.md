@AGENTS.md

## 보고 언어 규칙 (고정)

- 모든 진행 상황 보고와 최종 보고는 반드시 한국어로 작성한다.
- 코드·커밋 메시지·파일 경로·명령어는 원문 그대로 두되, 설명과 결과 요약은 한국어로 쓴다.

## 공개 상태 안전 규칙 (2026-09-24 긴급 정리 이후)

- 사용자 명시적 승인 전에는 Naver/Blogger 신규 발행·재공개·공개 글 수정을 하지 않는다.
- 자동 얼굴 점수(ArcFace)만으로 이미지를 승인하지 않는다. 최종 승인은 사용자가 이미지를 보고 한다.
- 새로 생성한 이미지는 검수 대기 폴더(`public/atlas/<channel>/<character>/_review/`)에만 둔다.
- 기존 공개 글의 본문과 URL은 보존한다. 비공개 전환 전에는 `data/atlas/backups/`에 백업한다.
